# Backyard Basketball

This site is for information and scheduling only. It does not include payments, checkout, or Stripe integration.

## Local development

- Install dependencies with `npm install`
- Start the app with `npm run dev`
- Build for production with `npm run build`

## Environment

Set up your Supabase values in `.env.local` for the frontend and edge functions. No Stripe keys are required.
